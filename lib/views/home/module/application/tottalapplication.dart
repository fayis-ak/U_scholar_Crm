import 'package:flutter/cupertino.dart';
import 'package:uscholarcrm/views/widget/text.dart';

class TottalApplication extends StatelessWidget {
  const TottalApplication({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        AppText('TottalApplication'),
      ],
    );
  }
}
